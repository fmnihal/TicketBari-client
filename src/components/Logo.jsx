import React from 'react';
import { Ticket } from 'lucide-react';

const Logo = () => {
    return (
        <div>
            <a className="btn btn-ghost text-xl">TicketBari <Ticket></Ticket></a>
        </div>
    );
};

export default Logo;